# Environment Variables

# Convex Configuration
VITE_CONVEX_URL=https://your-deployment.convex.cloud
CONVEX_DEPLOYMENT=dev:your-deployment
VITE_CONVEX_SITE_URL=https://your-deployment.convex.site

# JWT Secret (REQUIRED FOR PRODUCTION)
# Generate a secure random string (minimum 32 bytes)
# Example: openssl rand -base64 32
# WARNING: Never commit this value to version control!
JWT_SECRET=your_secure_random_secret_key_here

# Security Notes:
# 1. JWT_SECRET must be set in production environments
# 2. Use a cryptographically secure random string
# 3. Keep this secret safe and never expose it
# 4. Rotate the secret periodically for enhanced security
#


#arcjet
VITE_ARCJET_KEY=SAMPLE_arcjet_key
