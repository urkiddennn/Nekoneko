---
trigger: always_on
---

1. when new component library added, add it to the docs
2. when new updates on components, like styles props add it to the docs
3. Don't use border-radius large or extra large, use minimal on components if possible add a styles to customize it.